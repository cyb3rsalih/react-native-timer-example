import { StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
    container: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
      },
      button: {
          borderColor: '#000', 
          borderWidth:2, 
          borderRadius:30, 
          padding:30, 
          margin:5,
          width:160
        }
  });